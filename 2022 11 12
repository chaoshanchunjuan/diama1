#include <stdio.h>
#include <math.h>
#pragma warning (disable:4996)
int main()
{


	//	 
	//	 char arr[1000000]="";
	//	 int o=0;
	//	 scanf("%s", arr);
	//	 for (int n = 0; n < 1000000; n++)
	//	 {
	//		 /* if (arr[n] % 2 == 0) arr[n] == 0;
	//		  else if (arr[n]%2 != 0) arr[n] == 1;
	//		  if (n % 2 == 0) n == 0;
	//		  else if (n % 2 != 0) n == 1;
	//		  if (arr[n] = n)
	//			  arr[n] == 1;
	//		  else if (arr[n] != n)
	//			  arr[n] == 0;
	//		  int k=*/
	//		 if ((arr[n] % 2 )== ((n+1) % 2)) arr[n] = 1;
	//		 else arr[n] = 0;
	//		 for (int l = 1; l < 1000000; l++)
	//		 {
	//			 int k = pow(2, (l - 1)) * arr[l-1];
	//			
	//			 o = o + k;
	//		 }
	//
	//	 }
	// 
	//printf("%d", o) ;

	int arr[10][10];
	for (int i = 0; i < 10; i++)
	{
		for(int j = 0; j < 10;j++ )
		{
			scanf("%s", arr);
		}
	}
			printf("%s", arr);
	char arr3[3][100];
	for (int i = 0; i < 3; i++)
	{
		scanf("%s", arr[i]);
	}
	for (int k = 0; k < 3; k++)
	{

	
	printf("%s", arr[k]);
}
return 0;
}
